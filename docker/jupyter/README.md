# Процедура сборки docker-контейнера для запуска Jupyter-ноутбука

- Внести необходимые изменения в Dockerfile
- Увеличить версию (`version`) в `build.sh`
- Выполнить `./build.sh`, предварительно сделав `docker login`
